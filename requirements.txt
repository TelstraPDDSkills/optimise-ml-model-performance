# Build toolchain for the JupyterLite static site (used by the GitHub Actions CI).
# These run on the *build machine* only — the audience needs nothing installed.
# Pinned to versions verified locally on 2026-06-16.
jupyterlite-core==0.6.4
jupyterlite-pyodide-kernel==0.6.1
jupyter-server==2.18.2
